apiVersion: 1
datasources:
  - name: influxdb-cloud
    type: influxdb
    access: proxy
    url: ${INFLUXDB_URL}
    jsonData:
      version: SQL
      dbName: GPS
      httpMode: POST
      insecureGrpc: false
    secureJsonData:
      token: ${INFLUXDB_TOKEN}
    isDefault: true